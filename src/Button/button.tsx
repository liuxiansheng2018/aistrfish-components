import React from 'react';
import { Button } from 'antd';
import 'antd/dist/antd.css';

function InternalButton(props: any) {
  return <Button type="primary">{props.title}89</Button>;
}

export default InternalButton;
